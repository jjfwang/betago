# URL of the BetaGo backend API server.
# During development the Next.js dev server proxies /api/* to this URL.
# In production, configure your reverse proxy instead.
BACKEND_URL=http://localhost:3000
